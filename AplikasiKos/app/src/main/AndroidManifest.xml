<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.polije.sipakost.aplikasikos">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:layout_gravity="center"
        android:allowBackup="true"
        android:icon="@mipmap/ic_kost"
        android:label="SI-PAKOST"
        android:roundIcon="@mipmap/ic_kost"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".SignupActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="SI-PAKOST"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TambahActivity"
            android:label="SI-PAKOST"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TampilKosActivity"
            android:label="SI-PAKOST"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DetailKosActivity"
            android:label="SI-PAKOST"
            android:theme="@style/AppTheme" />

        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="SI-PAKOST" />
        <activity
            android:name=".TambahProfilActivity"
            android:label="SI-PAKOST"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".HomePencariActivity"
            android:label="SI-PAKOST"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TampilProfilActivity"
            android:label="SI-PAKOST"
            android:theme="@style/AppTheme" />
    </application>

</manifest>